import { authApi } from "@/lib/auth";
import { getAPIBaseURL } from "@/lib/config";
export type OnPageSeoWorkspace={pages:Array<{id:string;page_number:string;status:string;revision:number}>;versions:Array<{id:string;version_number:string;page_id:string;status:string;revision:number}>;releases:Array<{id:string;release_number:string;page_id:string;status:string;available:boolean;revision:number}>;availability:{application_id:"trust.onpage";status:"pilot"|"available";release_version:string|null}};
async function request<T>(path:string,init?:RequestInit){let token=authApi.getStoredToken();if(!token&&await authApi.restoreLocalDemoSession("hq"))token=authApi.getStoredToken();const headers=new Headers(init?.headers);headers.set("Content-Type","application/json");if(token)headers.set("Authorization",`Bearer ${token}`);const r=await fetch(`${getAPIBaseURL()}${path}`,{...init,headers});if(!r.ok)throw new Error((await r.json().catch(()=>({}))).detail||`页面SEO请求失败 (${r.status})`);return r.json() as Promise<T>};const b=(p:number)=>`/api/v1/factory-platform/projects/${p}/onpage-seo`;const post=<T>(path:string,payload:object)=>request<T>(path,{method:"POST",body:JSON.stringify(payload)});
export const listOnPageSeoWorkspace=(p:number)=>request<OnPageSeoWorkspace>(b(p));export const createOnPageSeoPage=(p:number,x:object)=>post(`${b(p)}/pages`,x);export const draftOnPageSeoVersion=(p:number,id:string,x:object)=>post(`${b(p)}/pages/${id}/versions`,x);export const reviewOnPageSeoVersion=(p:number,id:string,x:object)=>post(`${b(p)}/versions/${id}/review`,x);export const prepareOnPageSeoRelease=(p:number,id:string,x:object)=>post(`${b(p)}/versions/${id}/releases`,x);export const approveOnPageSeoRelease=(p:number,id:string,x:object)=>post(`${b(p)}/releases/${id}/approve`,x);export const acknowledgeOnPageSeoRelease=(p:number,id:string,x:object)=>post(`${b(p)}/releases/${id}/acknowledge`,x);
