# B2B 平台当前重构与扩展方案

本文件记录当前有效的大框架。目录与部署事实分别以 [工作区总入口](../README.md)、[源码入口](./README.md) 和 [部署中心](./deployment/README.md) 为准；本方案不再维护电脑盘符或七套源码副本。

## 目标

在一份源码上持续建设可本地测试、可分角色发布、可从 1 台平滑扩展到 7 台服务器的多租户 B2B 平台，覆盖：

- 总部端、代理源和客户源
- 多代理、多级代理运行端
- 多客户端以及客户端多个计划
- 多语言网站、内容、SEO、GEO、营销、CRM、询盘和 AI 能力
- 可审计发布、数据库迁移、素材隔离、观测、备份和灾难恢复

## 当前工作区模型

| 位置 | 当前职责 | 是否可直接编辑 |
|---|---|---|
| `00-platform-source` | 唯一源码、测试、迁移、蓝图和部署规则 | 是 |
| `01-hq-source-control` | 总部端、代理源、客户源的版本成品交付区 | 否 |
| `02-agency-runtime` | 代理运行端版本成品交付区 | 否 |
| `03-client-plan-runtime` | 客户与计划运行端版本成品交付区 | 否 |
| `04-content-worker` | 素材、构建、发布和异步任务版本成品交付区 | 否 |
| `05-edge-observability` | 网关、HTTPS、域名和观测版本成品交付区 | 否 |
| `06-data-services` | PostgreSQL、Redis、对象存储和迁移交付区 | 只编辑定义，不存真实生产数据 |
| `07-backup-disaster-recovery` | 异地备份、恢复演练和灾备交付区 | 只编辑定义，不存源码副本 |
| `local-data` | 本机数据库、私有素材、预览、备份暂存和迁移档案 | 仅本机运行链使用 |
| `local-runtime` | 可重建的本机 Node/Python、依赖、日志、PID 和开发密钥 | 由沙盘脚本维护 |

外层角色目录只保存交付元数据和未来的 `releases/<version>` 不可变成品。开发、修复和共享契约变更必须回到 `00-platform-source`。

## 应用与租户边界

- `zbcx`、`dlcx`、`khcs` 分别是总部、代理、客户应用壳，不是三套独立平台源码。
- `modules` 保存可复用业务能力，`shared/contracts` 保存跨模块契约。
- 客户计划是配置、内容、品牌和授权扩展，不复制客户端应用。
- 所有租户数据持续携带并校验 `agent_path`、`tenant_id`、`client_id` 和可选 `plan_id`。
- 总部端分别维护代理源和客户源；两条来源分支不能互相发布，也不能绕过来源端直接覆盖运行实例。

模板、实例和发布的具体关系见 [模板快照架构](./frontend/docs/template-snapshot-architecture.md)。

## 路径与数据规则

- 源码中的 `backend/data_models/path_registry.json` 只保存可移植示例。
- 当前电脑的路径注册配置位于工作区 `local-data/config/path-registry.json`，使用工作区相对值。
- 本地 SQLite 仅用于沙盘、迁移核对和验收；生产使用 PostgreSQL。
- 生产数据库、Redis、私有对象存储和备份由角色 `06`、`07` 的外部环境配置提供，不打进程序包。
- 上传素材原件只进入私有对象存储；网站公开成品与私有素材分离。
- `local-data/backup-staging` 是本机暂存，不是正式异地备份。

换电脑或换盘时移动完整工作区根目录，并让 PathRegistry 和沙盘脚本从当前位置重新解析；不得在源码中新增盘符常量。

## 技术路线

- 前端：React、TypeScript、Vite
- 后端：Python、FastAPI、SQLAlchemy、Alembic
- 本地数据库：SQLite
- 生产数据库：PostgreSQL
- 缓存、队列和限流：Redis
- 私有文件：对象存储或受控私有挂载
- 公网入口：HTTPS 网关、反向代理、WAF/CDN 连接器
- 本地维护：PowerShell 与 Python 验证工具

应用语言固定为 Python 和 TypeScript。新增基础设施通过配置和部署契约接入，不把供应商地址、密钥或机器路径写进业务代码。

## 开发线路

1. 在唯一源码中确认产品蓝图、共享契约和租户边界。
2. 修改对应应用壳、模块或平台能力。
3. 运行源码锁、类型、后端、共享契约和租户隔离检查。
4. 通过 `local-runtime` 启动沙盘，验收总部、代理源、客户源、代理端、客户端和计划。
5. 把稳定能力、迁移和回滚说明纳入同一候选版本。

## 发布线路

1. 发布预检。
2. 选择 `deployment/profiles` 中的 1—7 台服务器组合。
3. 按 `deployment/role-definitions` 从唯一源码生成目标角色包。
4. 审核 manifest、校验值、环境变量、迁移、客户影响和回滚版本。
5. 只部署所选 profile 需要的不可变角色成品。
6. 完成三端、素材、队列、数据、入口、观测和恢复证据检查；失败立即回滚。

完整流程、发布包结构和固定原则见 [源码与部署中心](./deployment/README.md)。当前 dry-run 工具只生成计划，不创建发布包或连接服务器。

## 1—7 台扩展原则

- 1 台用于本地、演示或低风险试运行，必须另配外部备份。
- 2—6 台依次拆分数据、worker、入口、总部控制、代理运行和客户计划运行。
- 7 台增加位于不同凭据和故障域的专用灾备节点。
- 扩容只改变角色放置，不复制源码，不修改租户标识，不迁移客户数据归属。

机器可读映射只维护在 `deployment/profiles/01-server.yaml` 至 `07-server.yaml`。

## 分阶段建设

### 底座收敛

- 保持唯一源码和七角色契约稳定。
- 清理本地存储与真实 API 混用。
- 完成 PathRegistry、租户上下文、迁移和私有素材边界。

### 业务闭环

- 把总部、代理源、客户源以及运行实例接入真实模板、版本和审计数据。
- 逐步打通内容、获客、询盘、CRM、订单、客户成功和经营分析。
- 每个应用以工厂平台契约中的可演示证据和交付状态为准。

### 生产准备

- 使用 PostgreSQL、Redis、私有对象存储和专用 worker。
- 完成容器、网关、观测、备份、恢复演练和安全评估。
- 按总部、测试代理、测试客户计划、全量的发布环逐级放量。

## 不可改变的原则

- 不以复制目录代替模板、租户或发布模型。
- 不把业务数据、素材和备份放进源码或镜像。
- 不在服务器直接修源码或手改不可变成品。
- 不因文档中的历史示例恢复旧目录结构。
- 不把规划能力表述为已经交付；状态以程序事实源和当前验收证据为准。

